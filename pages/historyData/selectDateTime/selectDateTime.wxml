<!--pages/historyData/selectDateTime/selectDateTime.wxml-->
<demo-block title="选择完整时间">
  <van-datetime-picker
    type="datetime"
    data-type="datetime"
    loading="{{ loading }}"
    value="{{ currentDate1 }}"
    min-date="{{ minDate }}"
    bind:input="onInput"
  />
</demo-block>
