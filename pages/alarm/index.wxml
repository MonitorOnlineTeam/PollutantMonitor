<!-- <view class="alarm-container">
  <l-card wx:for="{{alarmDataList}}" type="primary" plaintext="{{true}}" l-class="alarm-card">
    <view class="alarm-title">
      <l-tag l-class="alarm-tag" plain="{{true}}" font-color="{{item.AlarmType === '2' ? '#ff4d4f' : '#faad14'}}">{{item.AlarmTypeName}}</l-tag>
      {{item.AlarmTime}}
    </view>
    <view class="content">
      {{item.AlarmMsg}}
    </view>
  </l-card>
</view> -->
<view class="alarm-container">
  <l-card wx:for="{{alarmDataList}}" type="primary" plaintext="{{true}}" l-class="alarm-card">
    <view class="alarm-title">
      <l-tag l-class="alarm-tag" plain="{{true}}" font-color="{{item.alarmType === '2' ? '#ff4d4f' : '#faad14'}}">{{item.alarmTypeName}}</l-tag>
      {{item.alarmTime}}
    </view>
    <view class="content">
      {{item.alarmMsg}}
    </view>
  </l-card>
  <View style="height:106rpx"></View>
  <!-- <l-dialog 
    show="{{noSubscribe}}"
    type="confirm"     
    title="提示"
    content="需要订阅才能接受小程序推送！"
    bind:linconfirm="confirmSubscribeMessage"
    bind:lincancel="cancelSubscribeMessage"
     /> -->
</view>

